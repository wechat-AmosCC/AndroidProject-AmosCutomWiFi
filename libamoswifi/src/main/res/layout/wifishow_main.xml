<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/search_btn"
        android:layout_width="160dp"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="5dp"
        android:background="@drawable/wifiserch"
        android:text="@string/wifi_serch"
        android:textAlignment="center"
        android:textSize="16sp"></Button>

    <ListView
        android:id="@+id/wifi_lv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/search_btn"
        android:layout_marginTop="10dp"
        android:divider="#d1d1d1"
        android:dividerHeight="1px"
        android:scrollbars="none" >
    </ListView>

    <Button
        android:id="@+id/showwifi_close"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignTop="@+id/search_btn"
        android:layout_alignParentStart="true"
        android:layout_marginTop="5dp"
        android:layout_marginStart="10dp"
        android:background="@drawable/wifiback" />

    <TextView
        android:id="@+id/showwifi_title"
        android:layout_width="150dp"
        android:layout_height="40dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="13dp"
        android:text="@string/wifi_not_connect"
        android:textAlignment="center"
        android:textSize="18sp" />

</RelativeLayout>